# Hoisted layout: required for React Native / Metro (no symlinked node_modules),
# and keeps a single copy of react across web + mobile.
node-linker=hoisted

# Keep the pnpm store inside the repo so installs behave identically everywhere.
store-dir=.pnpm-store
